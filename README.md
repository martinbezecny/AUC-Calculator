A very simple program to calculate area under curve using the trapezoidal rule.

Input [x, y] point coordinates and click 'Calculate'. The program automatically sorts points on the x axis in ascending order.
